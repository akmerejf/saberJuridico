<%= simple_form_for @artigo, :html => { :class => 'form-horizontal',:multipart => true } do |f| %>
		<%= f.input :titulo %>
  		<%= f.input :autor %>
 		<%= f.file_field :pdf %>
  		<%= f.button :submit, value: "Enviar" %>
 <% end %>
  
