<%= simple_form_for @volume, :html => { :class => 'form-horizontal',:multipart => true } do |f| %>
  <% if @volume.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@volume.errors.count, "error") %> prohibited this volume from being saved:</h2>

      <ul>
      <% @volume.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :edicao %>
    </div>
  <%= f.fields_for :articles do |builder| %>
 
    <%render 'artigo_articles', f: builder %>
    
    <% end %>

    <%= link_to_add_articles "Adicionar artigo", f, :articles %>
  <div class="actions">
   <%= f.button :submit %>
  </div>
<% end %>
